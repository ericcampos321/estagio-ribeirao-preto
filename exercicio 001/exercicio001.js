let INDICE = 13;
let SOMA = 0;
let K = 0;

for (let K = 1; K <= INDICE; K++) {
    SOMA += K;
}

console.log(SOMA);

/* foi feito um parâmetro INDICE e utiliza um loop for para calcular a soma dos números de 1 até o valor de INDICE. O resultado é então retornado pela função e impresso no console*/